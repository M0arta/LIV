% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liv_computation.R
\name{compute_LIV_curve}
\alias{compute_LIV_curve}
\title{Compute LIV Estimates over a Grid}
\usage{
compute_LIV_curve(
  data,
  instrument_grid,
  outcome_resid_var,
  treatment_resid_var,
  instrument_resid_var,
  bw = 0.05,
  kernel = "epanechnikov"
)
}
\arguments{
\item{data}{A data frame containing residualized data with outcome, treatment, and instrument residuals.}

\item{instrument_grid}{A numeric vector specifying grid points for the residualized instrument.}

\item{outcome_resid_var}{A string specifying the name of the residualized outcome variable (e.g., "Y_resid").}

\item{treatment_resid_var}{A string specifying the name of the residualized treatment variable (e.g., "D_resid").}

\item{instrument_resid_var}{A string specifying the name of the residualized instrument variable (e.g., "Z_resid").}

\item{bw}{Bandwidth for non-parametric regression. Default is \code{0.05}.}

\item{kernel}{Kernel type for non-parametric regression. Options include \code{"epanechnikov"}, \code{"gaussian"}, etc. Default is \code{"epanechnikov"}.}
}
\value{
A numeric vector of LIV estimates corresponding to each point in \code{instrument_grid}.
}
\description{
Computes Local Instrumental Variable (LIV) estimates at specified instrument grid points using non-parametric regression.
}
\examples{
\dontrun{
LIV_estimates <- compute_LIV_curve(
  data = mroz_filtered,
  instrument_grid = instrument_grid,
  outcome_resid_var = "Y_resid",
  treatment_resid_var = "D_resid",
  instrument_resid_var = "Z_resid",
  bw = 0.05,
  kernel = "epanechnikov"
)
}
}
